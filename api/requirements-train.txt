# ─────────────────────────────────────────────────────────────────────────────
# Bug Predict — Training-only dependencies
# ─────────────────────────────────────────────────────────────────────────────
# These packages are ONLY needed to run api/train_model.py locally.
# They are NOT deployed to Vercel. Do not add them to requirements.txt.
# Run training with: uv run python -m api.train_model
# ─────────────────────────────────────────────────────────────────────────────
pandas==2.2.1
scikit-learn==1.4.1.post1
xgboost==1.7.6
numpy==1.26.4
